# CHANGELOG

이 프로젝트의 모든 주요 변경사항을 기록합니다.

버전 형식: `YY.메이저.마이너`
- `YY`: 연도 두 자리 · `메이저`: 새 기능 추가 시 증가(마이너 0으로 초기화) · `마이너`: 버그 수정·내부 변경 시 증가
- 예) `26.7.0`

> `26.7.0` 이전 버전은 CalVer(`vYY.MM.NN`)를, `v26.03.01` 이전(~2.4.x)은 Semantic Versioning을 사용했습니다.
> 직전 릴리즈 `v26.06.02` 다음 릴리즈부터 위 형식을 적용합니다.

---

## [26.10.1] - 2026-09-23

고객사 담당자 지정 변경을 모든 사내 사용자에게 허용하고 변경 이력을 보강한 마이너 릴리즈입니다.

### Changed

- **담당자 지정 변경 권한 확대**: 관리자가 아닌 사용자의 정·부 담당 엔지니어와 담당 영업 변경이 오류 없이 무시되던 제한을 제거해 모든 사내 사용자가 변경할 수 있도록 변경 (고객사 삭제 등 관리자 전용 기능은 유지)
- **담당자 변경 이력 보강**: 기존에는 담당 엔지니어의 이전 이름만 기록하던 서비스 로그를 정·부 담당 엔지니어와 담당 영업 모두 `이전 → 변경` 이름으로 기록하도록 개선

---

## [26.10.0] - 2026-09-23

업그레이드 계획 진척 현황 기록과 체크리스트 검증 담당자 지정·검증 대기 알림을 추가하고, 계약 시작 이전 월이 누락 점검서로 표시되던 문제를 수정한 메이저 릴리즈입니다.

### Added

- **업그레이드 진척 현황**: 업그레이드 계획 조회 화면 하단에 날짜·작성자·내용으로 진척 사항을 기록하는 목록 추가
  - 작성자는 로그인 사용자로 자동 입력되며 필요 시 수정 가능
  - 기록 수정·삭제는 실제 작성자 본인과 관리자만 가능 (서버에서도 권한 검증)
  - 업그레이드 계획이 없는 고객사는 첫 기록 추가 시 `미정` 상태의 계획을 자동 생성
  - 계획 수정 저장과 분리되어 계획을 수정해도 진척 기록이 유지됨
- **체크리스트 검증 담당자 지정**: 가상PC 이미지 체크리스트와 업그레이드 고려 사항에 검증 담당자를 지정하는 기능 추가
  - 후보는 기술팀 소속 활성 사용자이며 고객사 정·부 담당자가 아니어도 지정 가능
  - 조회 화면에 지정된 검증 담당자 표시
- **대시보드 검증 대기**: 일반 사용자 대시보드에 나에게 지정된 체크리스트 중 검토 완료·검증 전 건수를 체크리스트 단위로 표시하는 카드 추가
  - 카드 클릭 시 고객사·체크리스트 종류·대기 항목을 팝업으로 보여주고, 항목 클릭 시 해당 고객사의 구성 정보 또는 업그레이드 계획 화면으로 이동

### Changed

- 서비스 로그에 고객사를 ID 대신 고객사 이름으로 기록 (소스 관리 민감정보 열람, 회의록 작성, 점검 대상 추가·수정·삭제, 점검서 파일 보안 차단, 업그레이드 진척 현황 수정·삭제)

### Fixed

- **누락 점검서 판정에 계약 기간 반영**: 연중 1~12월 전체를 기준으로 판정해 계약 시작 이전 월(예: 7월 계약 시 1~6월)까지 누락으로 표시되던 문제 수정. 계약 시작월 이전과 계약 종료월 이후는 누락 판정에서 제외하며, 계약일이 없는 고객사는 기존과 동일하게 연중 전체를 판정

---

## [26.9.8] - 2026-09-22

Nginx 컨테이너의 IPv4 전용 수신 설정과 `localhost`의 IPv6 해석이 충돌해 프론트엔드와 프록시가 정상 실행 중에도 unhealthy 상태로 남던 문제를 수정한 마이너 릴리즈입니다.

### Fixed

- 프론트엔드와 프록시 healthcheck 주소를 `localhost`에서 IPv4 루프백인 `127.0.0.1`로 변경해 Alpine 환경에서 `::1` 연결 거부로 준비 상태 확인이 계속 실패하지 않도록 수정
- 온라인·오프라인 Compose 모두 IPv4 healthcheck를 사용하고 `localhost`가 다시 사용되지 않도록 회귀 테스트 보강

---

## [26.9.7] - 2026-09-22

느린 폐쇄망 호스트에서 프론트엔드와 프록시 초기화가 2분을 초과해 정상 업그레이드가 롤백되던 문제를 수정한 마이너 릴리즈입니다.

### Changed

- 프론트엔드와 프록시에 HTTP healthcheck를 추가하고, 프록시는 백엔드와 프론트엔드가 모두 정상 상태가 된 후 시작하도록 서비스 준비 순서를 강화

### Fixed

- 오프라인 업그레이드 스크립트가 고정된 2분 HTTP 재시도 대신 프록시 컨테이너의 실제 health 상태를 최대 10분간 확인하고, 정상 상태 확인 후 호스트 공개 포트를 추가 검증하도록 개선
- 프록시 상태 확인 실패 시 컨테이너 상태와 서비스 로그를 함께 남긴 뒤 롤백하도록 진단 정보 보강

---

## [26.9.6] - 2026-09-22

오프라인 업그레이드 직후 프록시 초기화가 완료되기 전에 상태 확인이 실행되어 정상 업그레이드가 롤백되던 문제를 수정한 마이너 릴리즈입니다.

### Fixed

- 프록시 헬스 체크가 일시적인 연결 초기화·거부에 한 번 실패하더라도 최대 60회, 2초 간격으로 재시도하고 제한 시간 내 계속 실패할 때만 로그 수집과 롤백을 수행하도록 개선

---

## [26.9.5] - 2026-09-22

내부 엔지니어의 고객사 정보 편집 범위를 바로잡고 폐쇄망 업그레이드의 마이그레이션 복구 절차를 안정화한 마이너 릴리즈입니다.

### Changed

- **내부 엔지니어 편집 범위 확대**: 고객사의 정·부 담당자로 지정되지 않은 로그인 사용자도 구성 정보, 유지보수 정보·점검 대상, 업그레이드 계획 및 지원 목록을 조회·등록·수정할 수 있도록 변경
- **지원 이력 감사 추적 유지**: 담당자 지정 여부와 무관하게 지원 목록을 처리할 수 있도록 하면서 작성자·수정자, 접속 IP 및 변경 전·후 데이터가 기존 감사 이력에 계속 기록되도록 유지
- 고객사 담당자 지정 정보 변경 등 별도 관리자 권한이 필요한 작업은 기존 권한 정책을 유지

### Fixed

- 구성 정보 편집 데이터를 불러오지 못했을 때 빈 입력 폼을 정상 데이터처럼 표시하던 문제를 수정하고, 저장할 수 없는 오류 상태를 명확히 표시
- 오프라인 업그레이드에서 Compose 이미지 변수의 기본값을 잘못 사용하지 않도록 대상 버전으로 이미지 참조를 해석하고, 이미지 로드 직후 필수 이미지와 버전 태그를 검증하도록 개선
- 기존 스키마가 있으나 Prisma 기준 마이그레이션 이력이 없는 환경에서 초기 마이그레이션이 `P3018`/MariaDB `1050`으로 충돌하는 경우 해당 기준 마이그레이션만 안전하게 적용 처리한 뒤 나머지 마이그레이션을 계속 수행하도록 개선
- 기준 마이그레이션이 이미 적용 처리되어 `P3008`이 반환되는 재실행 상황에서도 후속 마이그레이션을 계속 수행하도록 복구 절차를 멱등화

---

## [26.9.4] - 2026-09-21

업그레이드 계획의 공통 고려 사항 검증 저장 오류를 수정한 마이너 릴리즈입니다.

### Fixed

- **공통 고려 사항 검증 저장 안정화**: 설명 또는 비고가 입력되지 않아 API 응답에 `null`이 포함된 경우에도 검증 체크 후 정상적으로 저장되도록 편집 데이터를 빈 문자열로 정규화
- 업그레이드 계획 저장 시 선택 입력값을 null-safe 방식으로 정리해 `Cannot read properties of null (reading 'trim')` 오류가 발생하지 않도록 수정

---

## [26.9.3] - 2026-09-21

업그레이드 계획과 가상PC 이미지 체크리스트의 변경 이력 및 2단계 검토·검증 절차를 보강한 마이너 릴리즈입니다.

### Added

- **업그레이드 계획 수정일 표시**: 업그레이드 계획 화면의 고객사명 옆에 계획 자체의 최종 수정 날짜와 시간을 표시
- **가상PC 이미지 체크리스트 2단계 확인**: 각 체크리스트 항목에 검토와 검증 체크박스를 제공하고, 저장 시 검토자·검증자의 사용자 이름과 처리 시각을 자동 기록
- 가상PC 체크리스트 검토·검증 권한과 화면 표시를 검증하는 회귀 테스트 추가

### Changed

- **업그레이드 계획 편집 레이아웃 개선**: 수정 화면에서는 검토자·검증자 이름 열을 숨기고 체크박스만 표시해 설명 입력란의 폭을 확대
- **가상PC 이미지 상세·엑셀 개선**: 체크리스트 결과에 검토 상태·검토자와 검증 상태·검증자를 구분해 표시
- 기존 검토·검증 상태는 구성 정보 수정 시에도 유지하고, 검증은 검토 완료 후 다른 사용자만 수행할 수 있도록 데이터 무결성 강화

---

## [26.9.2] - 2026-09-21

엑셀 내보내기의 한글·브라우저 호환성을 보강하고, 업그레이드 계획의 검토·검증 절차와 고객사 정보의 수정일 표시를 개선한 마이너 릴리즈입니다.

### Added

- **업그레이드 계획 2단계 확인**: 공통 고려 사항마다 서비스 검토자와 별도 검증자 체크박스를 제공하고, 처리한 사용자의 아이디 대신 이름과 처리 시각을 표시
- **공통 고려 사항 기본 양식**: `기본 양식 사용하기` 버튼으로 클라이언트 7개, 관리서버(관리웹) 9개의 표준 점검 항목을 일괄 반영하며 기존 사용자 정의 항목은 유지
- **정보 수정일 표시**: 구성 정보와 유지보수 정보의 최종 수정일을 고객사명 옆에 표시하고, 가상PC 이미지별 최초 작성일과 수정일을 함께 표시
- 엑셀 한글 라운드트립, 업그레이드 계획 검토·검증, 기본 양식 및 날짜 응답을 검증하는 회귀 테스트 추가

### Changed

- 브라우저에서 생성하는 엑셀 파일 다운로드를 공통 유틸리티로 통합하고 Blob URL 해제를 지연해 다운로드 안정성을 개선
- 업그레이드 고려 사항의 기능명·설명·카테고리가 변경되면 기존 검토·검증 상태를 초기화해 변경된 내용을 다시 확인하도록 개선
- 구성 정보를 수정해 가상PC 이미지 레코드가 재생성되는 경우에도 최초 작성일을 보존하도록 변경

### Fixed

- Blob URL을 즉시 해제해 일부 브라우저에서 엑셀 파일이 비어 있거나 다운로드가 실패할 수 있던 문제 수정
- 가상PC 이미지 정보 엑셀 내보내기 감사 로그가 허용 목록 누락으로 저장되지 않던 문제 수정
- 업그레이드 계획 확인자에 로그인 아이디가 표시되던 문제를 사용자 이름이 우선 표시되도록 수정
- 내보낸 XLSX 파일의 한글 시트명·열 제목·데이터가 정상적으로 보존되도록 검증 및 보강

---

## [26.9.1] - 2026-09-18

TOTP 2차 인증과 인증 이력 관리를 보강하고, 세션 타임아웃 및 운영자 복구 흐름을 안정화한 마이너 릴리즈입니다.

### Added

- **폐쇄망 TOTP 2차 인증**: Google Authenticator·Microsoft Authenticator 호환 OTP 기능 추가
  - 시스템 설정에서 OTP 전역 ON/OFF 설정
  - 미등록 사용자의 로그인 후 QR 코드·수동 키 등록 절차 제공
  - 등록 사용자의 비밀번호 인증 후 OTP 검증
  - 동일 OTP 코드 재사용 방지
- **관리자 OTP 등록 초기화**: 사용자 관리 화면에서 권한에 따라 OTP 등록을 초기화하고 서비스 이력에 기록
- **OTP 실패 이력 구분**: 잘못된 OTP 입력을 로그인 이력에 `OTP 로그인 실패`로 기록

### Changed

- **이력 화면 분리**: 시스템 이력에서 로그인·로그아웃·로그인 시도 기록을 제외하고 로그인 이력 탭에서 통합 조회하도록 변경
- **세션 타임아웃 활동 판정**: 대시보드·지원 로그의 백그라운드 polling은 세션 활동으로 간주하지 않도록 변경
- **OTP 설정 접근 흐름**: OTP를 활성화한 미등록 관리자는 재로그인 후 OTP 등록을 완료하도록 설정 화면과 인증 흐름 정비
- **E2E 테스트 보강**: TOTP 등록·로그인·OTP 실패 이력·시스템 이력 분리를 격리된 Docker 환경에서 검증

### Fixed

- 세션 타임아웃 설정 후 백그라운드 조회만으로 유휴 세션이 계속 연장되던 문제 수정
- 슈퍼 관리자 계정에서 OTP 설정 조회 시 등록 전 상태를 권한 오류로 잘못 표시하던 문제 수정
- OTP 활성화 직후 설정 재조회로 인해 발생하던 예외 흐름 수정
- 로그인 실패 기록에 비밀번호 실패와 OTP 실패 원인을 구분 저장하도록 개선

### Security

- 사용자별 TOTP 비밀키는 기존 암호화 키를 사용해 암호화 저장하며 평문 OTP와 인증 코드는 로그에 저장하지 않음
- 테스트 코드에 시크릿·비밀번호 상수를 남기지 않도록 런타임 생성 및 CI 정적 검사 적용
- OTP 등록 초기화 시 기존 비밀키·등록 상태·사용 단계 정보를 함께 제거

---

## [26.9.0] - 2026-09-18

인증·파일·백업·배포 전반의 보안 경계를 강화하고, 운영 환경의 업그레이드 안정성과 검증 체계를 보강한 메이저 릴리즈입니다.

### Added

- **세션 타임아웃 정책**: 시스템 설정에서 유휴 세션 타임아웃을 10~60분 범위로 설정하고, 만료 1분 전 안내 팝업 표시 여부를 선택할 수 있도록 추가
  - 팝업은 60초부터 0초까지 실시간 카운트다운
  - `로그인 연장하기` 버튼으로 현재 세션을 연장
  - 서버에서도 `lastActivity`를 기준으로 세션 만료를 강제
- **파일 보안 검사**: 점검서·점검서 양식 업로드 시 확장자와 매직바이트를 검증하고, 격리 디렉터리에서 ClamAV 악성코드 검사를 수행
  - 검사 서비스가 준비되지 않은 경우 업로드를 허용하지 않는 fail-closed 정책 적용
  - 차단 결과를 보안 감사 로그에 기록
- **백업 파일 암호화**: DB·문서 백업을 별도 `BACKUP_ENCRYPTION_KEY` 기반 AES-256-GCM으로 암호화하고, 암호화 백업 파일 다운로드 이력을 기록
- **초기 관리자 부트스트랩**: 빈 DB 최초 설치 시에만 `.env`의 `INITIAL_ADMIN_PASSWORD`로 `admin` 계정을 생성
- **Backend 품질·E2E 검증 체계**: ESLint, 보안 회귀 테스트, 격리된 Docker API E2E 테스트 및 CI 품질 게이트 추가
- **마이그레이션·오프라인 업그레이드 지원**: Prisma 마이그레이션 사전 점검, 암호화 백업, 오프라인 이미지·DB 업그레이드 절차 자동화

### Changed

- **최초 비밀번호 변경 흐름**: 초기 비밀번호 또는 만료된 비밀번호로 로그인한 사용자는 대시보드로 이동하지 않고 로그인 화면에서 변경하도록 변경
- **고객사 데이터 조회 정책**: 사내 일반 사용자의 고객사·문서 조회는 유지하되, 고객사 정보 변경·점검 대상 관리·점검서 업로드 등 변경 작업은 담당자 및 관리자 권한으로 제한
- **소스 관리 민감정보 표시**: 기본 조회에서는 서버·인사연동 자격증명을 마스킹하고, 사용자가 클릭해 열람할 때만 전체 값을 표시하도록 변경
- **민감정보 열람 감사**: 자격증명 전체 열람 시 사용자·고객사·IP·시각을 보안 로그에 남기며 실제 비밀번호 값은 로그에 기록하지 않음
- **운영 설정·비밀번호 처리**: 비밀번호 초기화 API에서 기본 비밀번호를 응답하지 않도록 변경하고, 관리자 역할에 따른 계정 생성·비밀번호 초기화 권한을 명확히 적용
- **Compose 보안 기본값 제거**: DB 비밀번호, JWT/암호화 키, 백업 암호화 키를 필수 환경변수로 전환하고 공개된 기본값 사용을 차단
- **운영 마이그레이션 정책**: 프로덕션 컨테이너에서 추적되지 않은 `db push`를 금지하고, 커밋된 Prisma 마이그레이션만 적용하도록 변경

### Security

- **백업 자격증명 노출 제거**: 백업 스크립트에서 DB 비밀번호를 명령행 인자나 대화형 입력으로 노출하지 않고 컨테이너 내부의 안전한 인증 설정을 사용
- **백업 파일 접근 통제**: 백업 디렉터리·파일 권한을 제한하고 평문 임시 파일을 제거하며, 다운로드 대상은 암호화 백업 파일로 제한
- **암호화 무결성 강화**: 기존 AES-256-CBC 저장값과의 하위호환을 유지하면서 신규 저장값은 AES-256-GCM으로 전환해 변조를 탐지
- **업로드 저장 경계 강화**: 보안 검사 전 파일을 최종 고객사 디렉터리에 저장하지 않고 격리 영역에서만 처리
- **역할·권한 검증 강화**: 지원하지 않는 역할 생성을 차단하고 일반 관리자의 관리자·슈퍼 관리자 계정 생성 및 비밀번호 초기화를 제한
- **초기·DB 비밀번호 기본값 제거**: 저장소와 Compose 설정에서 `admin/1111`, 공개 DB 비밀번호 등 운영에 사용할 수 있는 기본값을 제거

### Fixed

- 마이그레이션 실행 결과와 실패 코드를 정확히 전달해, 예기치 않은 스키마 상태에서 컨테이너가 계속 기동하지 않도록 수정
- 기존 DB 업그레이드 시 마이그레이션 기준점과 사전 백업을 확인하도록 오프라인 업그레이드 절차 수정
- 업로드 보안 검사 및 권한 검증 실패 시 격리 파일과 임시 파일이 남지 않도록 정리
- 세션 갱신·로그아웃·중복 로그인 방지 로직이 설정된 타임아웃 정책을 일관되게 사용하도록 수정
- Backend 보안 테스트 52개와 Docker E2E 시나리오 3개로 초기 비밀번호, 권한, 백업, 파일 보안, 세션 정책을 검증

## [26.8.1] - 2026-09-17

26.8.0 전체 코드 보안·품질 리뷰에서 발견된 문제를 수정한 패치 릴리즈입니다. 서버 접속 자격증명 노출/유실과 관련된 항목이 다수 포함되어 있어 업그레이드를 권장합니다.

### Security

- **서버 접속 자격증명 평문 노출 차단**: 소스관리 조회 API(`GET .../source-management`)가 관리웹·SSH·root 패스워드와 인사연동 DB 비밀번호를 로그인한 모든 사용자에게 복호화해 반환하던 문제 수정. 이제 기본 조회는 마스킹(`********`)되고, 실제 값은 담당자·관리자 권한으로 감사 기록되는 별도 엔드포인트(`GET .../source-management/edit`)에서만 조회됨. 상세 화면의 "민감 정보 블라인드" 카드는 클릭 시 이 엔드포인트로 실제 값을 가져오도록 변경
- **암호화 방식 강화**: 무결성 검증이 없는 AES-256-CBC에서 AES-256-GCM(변조 감지 가능)으로 전환. 기존 CBC로 저장된 값은 계속 복호화 가능(하위호환), 손상되거나 변조된 값을 만나도 요청 전체가 실패하는 대신 안전하게 처리되도록 개선

### Fixed

- **비밀번호·연동정보 부분 수정 시 유실 방지**: 인사연동 DB 비밀번호 등 일부 값을 건드리지 않고 다른 항목만 수정해도 조용히 삭제되던 문제, 업그레이드 계획/인사연동 매핑/서버 정보/서버 접근 정보를 해당 항목 없이 저장하면 전량 삭제되던 문제 수정
- **디스크 구성 유실 방지**: 서버에 디스크 구성이 2개 이상 등록된 경우 소스관리 편집 화면에서 저장할 때마다 1개로 잘려 나가던 문제 수정
- **값 삭제 안 되던 문제 수정**: 업그레이드 계획의 버전/예상 일정, 소스관리 연동쿼리 필드를 비우고 저장해도 기존 값이 그대로 남아있던 문제 수정
- **서버 타임존 설정**: 백엔드 컨테이너에 `TZ=Asia/Seoul`을 명시해, 월 경계(매월 초/말)에서 점검 대상·완료 판정이 UTC 기준으로 흔들리던 문제 방지
- **입력 중 포커스 이탈 방지**: 서버 접근 정보·서버 목록에서 중간 항목 삭제 시 다른 행의 입력 포커스나 한글 조합 중인 텍스트가 엉뚱한 행으로 옮겨붙을 수 있던 문제 수정 (React key를 배열 인덱스 대신 안정적인 값으로 교체)

### Added

- 암호화 라운드트립·레거시 호환·변조값 안전처리를 검증하는 회귀 테스트(`crypto.test.cjs`) 추가

---

## [26.8.0] - 2026-09-17

업그레이드 계획 관리 기능을 새로 추가하고, 소스관리·점검 상태 판정 로직을 개선하며, 다크모드를 지원하는 메이저 릴리즈입니다.

### Added

- **업그레이드 계획 관리**: 고객사별 메이저 버전 업그레이드 계획을 관리하는 서브페이지 신설 (고객사 목록의 점검서·구성 정보 메뉴 사이에 배치)
  - 버전 관리(현재 버전 → 업그레이드 버전), 예상 일정(연/월 또는 분기 자유 입력)
  - 공통 고려 사항을 클라이언트/관리서버(관리웹) 카테고리로 분리, 기능·설명·확인여부·확인자·비고 항목 관리
  - 확인여부 체크 시 로그인 계정을 확인자로 자동 기록 (기존 체크 상태 유지 시 확인자 보존)
  - 고객사별 커스텀 항목(공통 항목에 해당하지 않는 개별 고려 사항) 별도 관리
  - 일정 상태(예정/미정/완료) 토글, 목록 화면에서 예정은 주황색·완료는 초록색으로 표시
- **다크모드 지원**: 우측 상단(알림 왼쪽)에 라이트/다크 모드 토글 추가. 기본은 시스템 설정(`prefers-color-scheme`)을 따르며, 선택한 모드는 브라우저에 저장되어 다음 접속에도 유지됨
- **대시보드 메뉴 추가**: 담당 고객사 카드에 회의록 보기 버튼 추가 (지원 목록과 점검서 보기 사이)

### Changed

- **소스관리 인사연동 개편**: DB 연동 테이블 정보를 부서/사용자로 분리해 아코디언으로 표시, 연동 쿼리(사용자/부서) 입력란을 세로로 배치해 가독성 개선
- **서버 접근 정보**: root 패스워드를 root 접근 가능 여부와 무관하게 SSH 패스워드처럼 항상 표시하도록 수정 (기존엔 접근 불가능으로 설정된 경우 패스워드가 가려짐)
- **화면 레이아웃 엔진 정비**: MUI Grid 컴포넌트의 폭 지정(`xs`/`sm` 등)이 실제로는 적용되지 않던 문제를 근본 수정해, 이 컴포넌트를 쓰는 여러 관리 화면의 정렬·줄바꿈이 의도한 대로 동작하도록 개선

### Fixed

- **분기·반기·연1회 점검 조기 진행 오표시 수정**: 고객사 요청으로 점검 일정을 앞당겨 진행한 경우(예: 12월 예정 분기 점검을 11월에, 9월 예정 연1회 점검을 6월에 완료) 예정 월에 점검서가 없어 미진행으로 잘못 표시되던 문제 수정 — 점검 완료 판정을 주기 길이(매월 1개월/분기 3개월/반기 6개월/연1회 12개월)만큼의 기간으로 확장해 고객사 목록·대시보드 통계·미점검 고객사 목록에 동일하게 적용
- **다크모드 표시 오류 다수 수정**: 상단 메뉴 하이라이트, 대시보드 카드, 로그인 화면, 백업 관리 화면, 알림·중복 로그인·공지사항·점검 미진행 팝업, 서버 구성 민감정보 블라인드 처리 등에서 배경/테두리/텍스트 색상이 하드코딩되어 다크모드에서도 라이트 모드 그대로 표시되거나 텍스트가 보이지 않던 문제 수정

---

## [26.7.4] - 2026-09-16

고객사 관리 화면의 검색 상태를 페이지 이동 시 초기화하는 패치 릴리즈입니다.

### Fixed

- **고객사 검색 상태 잔류 수정**: 고객사 관리에서 검색한 뒤 다른 페이지로 이동하면 검색어·필터·페이지 번호가 초기화되어, 다시 진입할 때 전체 고객사 목록이 표시되도록 수정

---

## [26.7.3] - 2026-09-15

기존 설치를 데이터 보존 상태로 오프라인 업그레이드할 수 있도록 배포 자동화와 릴리즈 자산을 보강한 패치 릴리즈입니다.

### Added

- **오프라인 업그레이드 스크립트**: 이미지 번들 로드, MariaDB 및 운영 파일 백업, Compose 교체, 환경변수 보존, 헬스체크, 실패 시 설정 롤백을 자동화한 `offline-upgrade.sh` 추가
- **릴리즈 자산 추가**: 버전 태그 릴리즈에 `offline-upgrade.sh`를 자동 첨부

### Changed

- **오프라인 배포 문서 보강**: 기존 설치 업그레이드 절차, 백업 위치, 암호화 키 보존 주의사항 추가

---

## [26.7.2] - 2026-09-15

인증·백업 안정성, 프론트엔드 성능, 의존성 보안 및 배포 검증을 개선한 마이너 릴리즈입니다.

### Added

- **보안 회귀 테스트 추가**: 세션 만료·로그아웃·비밀번호 만료, 백업 실패 처리, 배포 구성, 프론트엔드 비밀번호 변경 흐름을 검증하는 테스트 21개 추가
- **비밀번호 만료 상태 전달**: 서버 응답에 `PASSWORD_EXPIRED` 오류 코드를 포함해 프론트엔드가 강제 변경 화면을 일관되게 표시

### Changed

- **세션 검증 강화**: JWT에 포함된 세션만 검증하고, 삭제·만료된 세션이 토큰 사용으로 되살아나지 않도록 활동 시간과 사용자·세션 ID를 함께 확인
- **중복 로그인 설정 반영**: 중복 로그인을 허용한 경우 기존 세션을 유지하고, 로그아웃은 요청한 세션만 종료
- **백업 요청 검증 강화**: 원격 백업 실행 전 SFTP 호스트·포트·계정·인증수단을 검증하고, 잘못된 설정은 파일 생성 전에 거부
- **예외·감사 로그 민감정보 보호**: 중첩 객체와 배열 내부의 비밀번호·토큰·시크릿을 마스킹하거나 제거
- **프론트엔드 초기 번들 최적화**: 페이지 lazy loading과 라이브러리별 청크 분리를 적용해 초기 JS 번들을 약 3.1MB에서 약 30KB로 축소
- **의존성 보안 업데이트**: `multer`, `sanitize-html`, `uuid` 및 관련 lockfile을 갱신하고 ExcelJS의 취약 UUID 의존성을 override. 백엔드 `npm audit`는 취약점 0건으로 정리
- **컨테이너 런타임 정렬**: Docker 빌드/실행 이미지를 Node 22로 통일하고 Prisma 클라이언트 생성 단계에 OpenSSL 추가
- **프론트엔드 오류·타입 처리 정리**: Axios 오류 메시지 처리 공통화, React Hook 의존성 정리, MUI Grid 호환 처리 및 lint 경고 제거

### Fixed

- **Prisma 마이그레이션 오류 처리 수정**: 파이프라인 종료 코드가 누락되던 문제를 수정하고, 초기 마이그레이션을 동적으로 탐색해 P3005 복구 후 재시도
- **비밀번호 만료 사용자 접근 제어 수정**: 비밀번호 변경·로그아웃 등 허용 경로를 제외한 API 접근을 서버에서 차단하고 프론트엔드 강제 변경 상태와 연동
- **원격 백업 실패 오표시·파일 잔류 방지**: SFTP 설정 오류를 사전에 반환하고, 백업 성공 여부와 임시 파일 정리를 일관되게 처리
- **프론트엔드 번들 크기 경고 수정**: 초기 번들 경고를 제거하고 ExcelJS·문서 내보내기·리치 텍스트 편집기 등을 필요한 화면에서만 로드

> 참고: 프론트엔드에는 공식 수정 릴리즈가 아직 없는 `quill@2.0.3`의 저위험 XSS advisory 1건이 남아 있습니다. 서버 측 HTML sanitization은 유지됩니다.

---

## [26.7.1] - 2026-08-31

오프라인(폐쇄망) 배포 편의성 개선 릴리즈입니다.

### Added

- **오프라인 이미지 번들을 GitHub Release에 자동 첨부**: 버전 태그 push 시 `backend`, `frontend`, `mariadb:10.11`, `nginx:alpine` 이미지를 `docker save` + gzip 하여 `customer-vault-images-<버전>.tar.gz`로 Release에 추가. 폐쇄망 서버에서 `docker load` 후 별도 이미지 리네임 없이 `docker compose up -d`로 바로 구동 가능

### Changed

- **`docker-compose.yml`에 온라인/오프라인 배포 안내 주석 추가**: 오프라인 환경에서는 이미지 번들을 `docker load` 후 그대로 사용하면 되며, `build:` 섹션은 `docker compose up`에서 무시되므로 소스 코드 없이 동작함을 명시. 옛 이미지 리네임(`customer_backend:...`) 주석 제거
- **Release 본문에 배포 절차 정리**: 온라인 신규 설치 / 온라인 업그레이드 / 오프라인 폐쇄망 3가지 시나리오
- **`scripts/export-package.sh`**: CHANGELOG 버전 추출 정규식을 `v` 접두사 없는 형식(`## [26.7.1]`)에 대응, 프론트엔드 빌드 인자에서 `v` 접두사 제거

### Fixed

- **Release의 `.env` 템플릿 파일명이 `default.env.example`로 바뀌어 다운로드 안내가 맞지 않던 문제 수정**: GitHub Release가 점(`.`)으로 시작하는 자산명을 허용하지 않아 발생. `env.example`이라는 이름으로 첨부하도록 변경

---

## [26.7.0] - 2026-08-31

전체 코드 리뷰 결과를 반영한 보안 강화·버그 수정 릴리즈입니다. 신규 기능은 없습니다.

> **배포 시 확인**
> - `.env`에 `JWT_SECRET`(권장 64 hex)와 `ENCRYPTION_KEY`(64 hex)를 **반드시 설정**해야 합니다. 미설정이거나 예시 기본값이면 서버가 기동되지 않습니다. 운영 중 기본값을 쓰고 있었다면 새 키 발급 → `.env` 반영 → 서버 접근정보·SFTP 비밀번호 재암호화 → 전체 세션 무효화가 필요합니다.
> - 일반 사용자(user)는 담당하지 않은 고객사의 회의록·지원 로그·점검 대상을 **수정/삭제할 수 없습니다** (조회는 종전과 동일).
> - 백엔드 의존성에 `sanitize-html`, `mime-types`가 추가되었습니다. 오프라인 배포 패키지 빌드 시 포함 여부를 확인하세요.

### Changed

- **공개된 기본 시크릿 사용 차단**: `JWT_SECRET`, `ENCRYPTION_KEY`가 미설정이거나 저장소/문서에 적힌 예시 기본값(`please-change-...`, 전부 `0` 등)이면 서버가 기동을 거부
  - `docker-compose.yml` / `docker-compose.offline.yml`에서 두 변수의 기본값 제거 (`${VAR:?...}`)
  - `.env.example`의 예시값을 빈 값으로 변경
- **편집 권한 정리**: 조회는 로그인한 모든 내부 사용자에게 열려 있으나, 아래 항목의 **생성·수정·삭제는 담당자(정/부 엔지니어, 영업) + 관리자만** 가능하도록 제한
  - 고객사 정보 수정, 소스/운영정보(형상관리), 회의록, 지원 로그, 점검 대상, 점검서 양식, 점검서 업로드
  - 일반 사용자는 고객사 수정 시 담당자 지정(`engineerId`/`engineerSubId`/`salesId`)을 변경할 수 없음
  - 공통 검사 유틸 `assertCustomerEditable` 신설
- **토큰 처리 강화**
  - Access / Refresh 토큰에 타입 클레임을 분리해 상호 재사용 차단
  - JWT 검증 알고리즘을 `HS256`으로 고정
  - 최초 로그인(초기 비밀번호 미변경) 사용자는 비밀번호 변경 전까지 다른 API 호출을 서버에서 차단
  - 비밀번호 해시 비용 상향 (bcrypt 10 → 12)
- **Swagger 문서 비노출**: `NODE_ENV=production`에서는 `/api/docs`를 마운트하지 않음
- **컨테이너 권한 축소**: 백엔드 컨테이너의 호스트 `/sys`, `/etc` 마운트 제거(대시보드 메모리 표시용 `/proc`만 유지), DB 포트를 `127.0.0.1`에만 바인딩
- **실시간 세션 이벤트(SSE) 재구성**: 연결이 끊기면 자동으로 재연결(백오프), 청크 경계에 걸친 이벤트 유실 방지, 환경변수 기반 경로/토큰 키 사용
- **토큰 갱신 요청 중복 제거**: 여러 요청이 동시에 만료를 감지해도 갱신은 한 번만 수행
- **고객사 목록 필터 상태 저장 범위 조정**: 브라우저 영구 저장 → 세션 단위 저장 (로그아웃·계정 전환 시 이전 검색어·필터가 남지 않음)
- **고객사 목록 스크롤 성능 개선**: 스크롤 중 불필요한 전체 리렌더 제거
- **릴리즈 파이프라인 버전 규칙 정비**: 버전 체계를 `YY.메이저.마이너`로 전환하고, GitHub Actions 워크플로우(`docker-build.yml`)의 태그 트리거·이미지 태그·Release 생성·CHANGELOG 추출을 `v` 접두사 없는 형식에 맞게 갱신. 프론트엔드 이미지에 버전 정보(`VITE_APP_VERSION`) 주입

### Fixed

- **저장형 XSS 취약점 수정**: 회의록·공지사항 HTML을 저장·조회 시 서버에서 살균(`sanitize-html`) 처리. `<script>`, 이벤트 핸들러, `javascript:` URL 등을 제거하고 서식만 유지
- **점검서 양식 업로드 경로 조작(Path Traversal) 취약점 수정**: 파일명의 경로 구분자·상위 경로·확장자를 검증하고, 최종 경로가 업로드 디렉토리 안에 있는지 재확인. 확장자 화이트리스트 및 업로드 크기 제한 추가
- **로그아웃 후에도 토큰이 유효하던 문제 수정**: Refresh 토큰 갱신 시 세션 존재 여부를 확인 → 로그아웃·다른 위치 로그인·관리자 세션 삭제 후에는 액세스 토큰이 재발급되지 않음
- **사용자 계정 정보 노출 수정**: `GET /users/:id` 상세 조회를 관리자 전용으로 변경 (계정 열거 차단)
- **로그인 시도 제한 우회 가능하던 문제 수정**: 신뢰하는 프록시 홉 수를 고정하고 클라이언트가 보낸 `X-Forwarded-For`를 직접 신뢰하지 않도록 변경 (브루트포스 제한 정상화, 감사 로그 IP 정확도 개선)
- **감사 로그 위조 가능하던 문제 수정**: 클라이언트 측 엑셀 내보내기 로그 기록 API(`POST /logs/excel-export`)에 입력 검증 추가 (`action` 화이트리스트, 길이 제한)
- **에러 로그에 비밀번호가 평문으로 남던 문제 수정**: 예외 로그에서 `password`/`token`/`secret`이 포함된 모든 필드를 마스킹 (`currentPassword`, `newPassword` 포함)
- **취약 의존성 제거**: `npm audit` High 항목 모두 해소
- **백업이 실패했는데 "성공"으로 기록되던 문제 수정**: `mysqldump`/`gzip` 종료 코드를 확인. 자격증명 오류·도구 부재 시 빈 백업 파일이 성공으로 기록되고 보관 정책이 정상 백업을 지우던 문제 해결
  - `DATABASE_URL` 파싱을 표준 URL 파서로 교체 (쿼리 파라미터·특수문자 비밀번호 대응)
  - DB 비밀번호를 명령행 인자 대신 환경변수(`MYSQL_PWD`)로 전달
- **백업 스케줄 시간대 문제 재수정**: 수동 UTC 변환 로직을 제거하고 cron 라이브러리에 `Asia/Seoul` 타임존을 직접 지정. 매월 1일 백업이 하루 늦게 실행되던 문제, 서버 타임존 설정에 따라 시각이 이중으로 밀리던 문제 해결
- **원격 전용 백업 시 로컬 디스크가 계속 차던 문제 수정**: SFTP 전용 설정에서도 전송용 임시 파일이 삭제되지 않아 볼륨이 가득 차던 문제 해결. 보관 개수 미설정 시 기본 7개 적용
- **로그인 실패 잠금이 설정과 무관하게 동작하던 문제 수정**: 관리자가 잠금 기능을 꺼도 실패 횟수 초과 시 계정이 잠기던 문제 해결 (`loginFailureLimitEnabled` 반영)
- **비밀번호 최대 길이 검증 미동작 수정**: 존재하지 않는 설정값을 참조해 상한 검증이 되지 않고, 오류 메시지에 "20자" 대신 "undefined자"가 표시되던 문제 해결
- **이력 날짜 필터 오류 수정**
  - 종료일을 지정하면 그날 오전 9시 이후 기록이 누락되던 문제(KST/UTC 9시간 차이) 해결
  - 시작일 또는 종료일 중 한쪽만 선택하면 필터가 조용히 무시되던 문제 해결
  - 시스템/로그인/업로드 이력 및 점검서 목록에 공통 적용
- **로그 이력 테이블에서 한 행을 펼치면 다른 행이 함께 펼쳐지던 문제 수정**: 서로 다른 두 테이블(서비스 로그·로그인 시도)의 PK가 겹쳐 발생하던 문제를 행마다 고유 키(`rowKey`) 부여로 해결
- **새로고침(F5) 시 로그아웃 처리되어 중복 로그인 방지가 무력화되던 문제 수정**: 창 닫힘 감지 요청이 서버 세션을 삭제하지 않고 기록만 남기도록 변경. 실제로 닫힌 세션은 비활성 30분 후 정리
- **백업 실행·대용량 파일 업로드/다운로드가 정상 완료돼도 "실패" 팝업이 뜨던 문제 수정**: 전역 10초 타임아웃에서 장시간 작업 제외 (120초)
- **점검서 뷰어 페이지 메모리 누수 수정**: PDF Blob URL이 해제되지 않아 여러 건 열람 시 메모리가 계속 증가하던 문제 해결
- **세션 만료 시 점검서 업로드 페이지가 흰 화면이 되던 문제 수정** (React 훅 규칙 위반)
- **공용 PC에서 로그아웃 후 다른 사용자로 로그인 시 이전 사용자 데이터가 잠깐 보이던 문제 수정**: 로그아웃·세션 만료 시 조회 캐시를 비우도록 변경
- **강제 비밀번호 변경 창 위에 공지 팝업이 겹치고, 닫아도 반복 표시되던 문제 수정**
- **점검서 업로드 중 DB 기록이 실패하면 파일만 서버에 남던 문제 수정**
- **일부 브라우저(Firefox·Safari)에서 파일 다운로드가 빈 파일로 저장되던 문제 수정**: Blob URL 해제 시점 지연
- **한글 파일명 다운로드 시 파일명이 깨지던 문제 수정** (RFC 5987 형식 파싱)
- **Prisma 클라이언트가 스키마보다 오래되어 빌드가 실패하던 문제 수정**: 빌드 전 자동 생성(`prebuild`)

### Removed

- **미사용 `xlsx` 패키지 제거**: 엑셀 처리는 기존 `exceljs`를 그대로 사용 (프로토타입 오염 취약점 제거)
- **죽은 코드 정리**: 마운트되지 않던 세션 검증 훅, 사용되지 않던 인증 스토어의 토큰 필드, 권한 가드의 요청별 콘솔 로그, 프론트엔드 개발용 콘솔 로그 제거

---

## [v26.06.02] - 2026-06-16

### Fixed
- **점검서 열람·다운로드 403 오류 수정**: 고객사 담당자/부담당자/영업담당자로 지정되지 않은 사용자가 `documents/{id}/view` 및 `documents/{id}/download` 접근 시 403 에러와 함께 "문서를 불러올 수 없습니다" 페이지가 표시되는 문제 수정
  - `DocumentsController`의 `view`, `download` 엔드포인트에서 `isUserAssignedToCustomer` 배정 확인 로직 제거
  - 로그인한 모든 사용자(user/admin/super_admin)가 점검서 열람 및 다운로드 가능

---

## [v26.06.01] - 2026-06-05

### Fixed
- **알림벨 실시간 미반영 문제 수정**: 고객사 지원 로그 추가/수정/삭제 후 상단 알림벨 카운트가 페이지 새로고침 없이 즉시 반영되지 않는 문제 수정
  - `CustomerSupportLogsPage`에서 지원 로그 변경 성공 시 React Query 캐시(`pending-notifications`) 즉시 무효화 처리
- **이력 구분 필터 미동작 문제 수정**: 시스템/로그인 이력 검색 시 구분(경고·오류·정상·정보) 필터를 선택해도 적용되지 않는 문제 수정
  - `loginAttempt` 테이블은 `logType` 컬럼이 없어 DB 쿼리 레벨 필터가 불가하므로, `serviceLog`와 `loginAttempt` 통합 후 메모리에서 `logType` 후처리 필터링 추가 (`getSystemLogs`, `getLoginLogs`)
- **수동 백업 실패 팝업 오표시 문제 수정**: 백업은 정상 완료(이력에 성공 기록)되었으나 상단 팝업에 "백업에 실패했습니다"가 표시되는 문제 수정
  - `runBackup` 서비스 메서드에서 `createServiceLog` 호출을 try-catch로 감싸 로그 기록 실패가 HTTP 응답 오류로 전파되지 않도록 처리
- **백업 스케줄 시간대 오류 수정**: 관리자가 KST 기준으로 설정한 백업 시각이 9시간 늦게 실행되는 문제 수정 (예: 02:00 설정 → 11:00 실행)
  - `buildCronExpression`에서 사용자 입력값(KST) → UTC 변환(−9시간) 적용
  - 시간이 음수가 되는 경우(0~8시 KST) 24시간 보정 및 요일/날짜 하루 앞당김 처리

---

## [v26.05.02] - 2026-05-29

### Added
- **지원 로그 제목 필드 추가**: 지원 목록에 간단한 제목을 입력할 수 있는 `제목` 필드 추가
  - 지원 목록 테이블에서 기존 `사용자 정보` 컬럼이 `제목` 컬럼으로 교체 표시
  - 지원 로그 추가/수정 폼에 `제목` 입력란 추가 (문의 내용 입력란 위에 위치)
  - 세부 내용 조회 다이얼로그에 `제목` 항목 추가 (문의 내용 위에 표시)
  - 지원 목록 엑셀 내보내기에 `제목` 열 추가
  - 기존 데이터는 `제목` 빈값으로 표시되며 수정 시 입력 가능

### Schema
- **`support_logs` 테이블 `title` 컬럼 추가**: `title VARCHAR(200) NULL`
  - 기존 레코드는 `NULL`로 유지 (UI에서 빈값으로 표시)

---

## [v26.05.01] - 2026-05-14

### Added
- **중앙화된 암호화 서비스 (`CryptoService`)**: AES-256-CBC 기반 암호화/복호화를 단일 서비스로 통합
  - `encrypt()` / `decrypt()` / `safeDecrypt()` 메서드 제공
  - `safeDecrypt()`: 암호화된 값과 평문 값 모두 처리 (마이그레이션 구간 역호환 보장)
  - `CryptoModule`로 분리하여 필요한 모듈에서 import하여 사용
- **서버 접근 정보 암호화 마이그레이션 스크립트**: 기존 평문으로 저장된 비밀번호를 일괄 암호화
  - `prisma/scripts/encrypt-server-access-info.ts`
  - 이미 암호화된 값은 건너뜀 (멱등성 보장)
  - `SystemSettings.sftpPassword` 동시 처리
- **전역 레이트 리밋**: `@nestjs/throttler` 적용
  - 전역: 분당 200 요청 (IP 기준)
  - 로그인 엔드포인트: 분당 10 요청으로 별도 제한 (브루트포스 방어)

### Changed
- **암호화 적용 범위 확대**: BackupService, SettingsService, CustomersService의 인라인 crypto 코드를 `CryptoService`로 교체
  - SFTP 비밀번호 암호화/복호화 (BackupService, SettingsService)
  - 서버 접근 정보(웹/SSH/Root 비밀번호) 암호화/복호화 (CustomersService)
- **`ServerAccessInfo` 컬럼 길이 확장**: `webPassword`, `serverSshPassword`, `serverRootPassword` VARCHAR(100) → VARCHAR(512)
  - AES-256-CBC 암호화 후 hex 인코딩 시 길이 증가 반영
- **CORS 보안 강화**: `NODE_ENV=production`에서 `CORS_ORIGIN` 환경변수 기반 화이트리스트 적용
  - 개발 환경: 전체 허용 유지
  - 운영 환경: 미설정 시 전체 차단
- **환경변수 시작 시 검증 강화** (`main.ts`):
  - `ENCRYPTION_KEY`: 64자리 hex 형식 검증, 미달 시 서버 시작 거부
  - `JWT_SECRET`: 32자 이상 검증, 미달 시 서버 시작 거부
- **문서 다운로드/뷰어 접근 권한 체크** (`documents.controller.ts`):
  - USER 역할: 담당 고객사(engineerId, engineerSubId, salesId)의 문서만 접근 가능
  - ADMIN/SUPER_ADMIN: 전체 접근 허용
- **파일 I/O 비동기화** (`documents.service.ts`): `fs.*Sync` 계열 전면 제거, `fs/promises` API로 교체
  - `existsSync` → `fsp.access()`, `mkdirSync` → `fsp.mkdir()`, `renameSync` → `fsp.rename()`, `unlinkSync` → `fsp.unlink()`
  - 고객사·점검대상 DB 조회 `Promise.all()` 병렬화

### Fixed
- **민감 정보 콘솔 노출 제거**: auth, customers, documents 모듈 전반의 `console.log` 전면 제거
  - 로그인 흐름, 세션 관리, 파일 업로드, 소스 관리 등 디버그 로그 약 50여 개 삭제

### Schema
- **DB 인덱스 추가** (Prisma schema): 주요 조회 패턴에 맞춘 복합/단일 인덱스 추가
  - `User`: `@@index([isActive])`
  - `Customer`: `@@index([engineerId])`, `@@index([salesId])`, `@@index([contractEndDate])`, `@@index([lastInspectionDate])`
  - `Document`: `@@index([customerId])`, `@@index([uploadedAt])`, `@@index([inspectionDate])`
  - `LoginAttempt`: `@@index([userId, attemptTime])`
  - `UserSession`: `@@index([userId, lastActivity])`

---

## [v26.03.04] - 2026-03-24

### Fixed
- **비밀번호 만료 시 로그인 불가 문제 수정**: 비밀번호 사용 기간 만료 시 로그인이 거부되던 문제 해결
  - 기존: 만료 시 403 에러 반환 → `alert()` 메시지만 표시되어 로그인 불가
  - 변경: 만료 시에도 로그인 허용(토큰 발급), 응답에 `passwordExpired: true` 플래그 포함
  - 로그인 직후 강제 비밀번호 변경 다이얼로그 자동 표시 (`isFirstLogin` 패턴과 동일)
  - "비밀번호 사용 기간이 만료되었습니다. 계속하려면 비밀번호를 변경해야 합니다." 경고 메시지 표시

---

## [v26.03.03] - 2026-03-20

### Added
- **회의록 Word 내보내기**: 작성된 회의록을 `.docx` 형식으로 내보내기 기능 추가
  - 표 형식 레이아웃: 날짜/장소/회의 주제/참석자/회의 내용/결정 사항/비고
  - 목록 행의 `내보내기` 버튼 및 보기 다이얼로그의 `Word 내보내기` 버튼 제공
  - 파일명: `회의록_[고객사명]_[날짜]_[주제].docx`

### Changed
- **회의록 참석자 입력 방식 개선**: 개별 항목 추가(최대 10명) 방식 → 자유 텍스트 입력(줄바꿈으로 구분) 방식으로 변경
- **회의록 작업 컬럼 너비 확대**: 내보내기 버튼 추가로 인한 줄바꿈 방지 (220px → 330px)

---

## [v26.03.02] - 2026-03-19

### Added
- **회의록 기능**: 고객사별 회의록 작성/조회/수정/삭제 기능 추가
  - 회의록 페이지 (`/customers/:id/meeting-minutes`)
  - 작성 양식: 날짜(캘린더 DatePicker), 위치, 회의 주제, 참석자(최대 10명 동적 입력), 회의 내용/결정 사항/비고(Rich Text 편집기)
  - 목록 테이블: 날짜, 회의록 주제, 작성자 컬럼 + 보기/수정/삭제 버튼
  - 고객사명 표시: 페이지 제목 `고객사명 - 회의록` 형식

### Changed
- **고객사 관리 컬럼 개편**
  - `전체 정보(내보내기)` 컬럼 → `회의록(보기)` 컬럼으로 변경 (회의록 페이지 이동)
  - `점검서보기` → `점검서` 컬럼명 통일
  - `세부사항보기` → `세부사항` 컬럼명 통일
  - 고객사명 hover 시 `전체 정보 내보내기` 버튼 표시 (엑셀 다운로드 기능 유지)
- **버전 체계 전환**: Semantic Versioning → CalVer (`vYY.MM.NN`)
  - `.env` VERSION, GitHub Actions 태그 패턴, Makefile, RELEASE_GUIDE 전면 업데이트
  - `export-package.sh`: 버전 인자 없을 시 CHANGELOG.md에서 최신 버전 자동 추출
- **레이아웃 개선**: Footer가 항상 페이지 하단에 고정되도록 수정
- **세션 만료 자동 처리**: 브라우저 종료 시 30분 비활동 후 세션 자동 만료
  - 백엔드 5분 주기 cron으로 만료 세션 정리
  - 로그인 시 만료 세션은 중복 로그인으로 처리하지 않음
- **`.gitignore`**: `.omc/` 디렉토리 추가

### Fixed
- Docker Compose 이미지 태그 `.env`의 `VERSION` 변수 자동 참조하도록 수정

---

## [v26.03.01] - 2026-03-19

### Added
- **백업 기능**: DB 및 업로드 파일(점검서) 자동/수동 백업 지원
  - 즉시 실행, 이력 조회, 파일 다운로드 (`/backup`)
  - 백업 주기/항목/경로/SFTP 설정 (시스템 설정 섹션)
- **JIRA 연동**: 지원 로그에 JIRA 티켓 입력 및 바로가기 연결
  - 시스템 설정에 JIRA URL 활성화 항목 추가 (`jiraEnabled`, `jiraBaseUrl`)
- **서버 접근 정보**: 고객사 상세에서 서버 접근 정보 기록 기능 추가
  - 민감 정보 블러(blur) 표시 처리

### Changed
- 형상 관리 OS 타입/종류 항목 통합, 시리얼 넘버 항목 추가
- 계약 정보에 POC(Proof of Concept) 항목 추가
- 알람 항목에 지원중 / 진행 불가 / 보류 상태 모두 표시
- 지원 중인 고객사 목록을 최신 이슈 순으로 정렬
- 공지사항 팝업에 "다시 보지 않기" 적용 개선
- Footer에 버전 정보 표시

### Fixed
- 사용자(user) 계정에서 JIRA 티켓 조회 불가 오류 수정
- `spawn mysqldump ENOENT` 에러 수정 (백업 실행 경로 문제)
- 전체 정보 내보내기 시 지원 로그 누락 오류 수정
- 일부 메뉴 접속 시 400 에러 발생 수정
- 점검서 보기 페이지 / 공지사항 팝업 400 에러 수정
- 이미 작성된 공지사항 수정 불가 오류 수정

---

## 이전 버전 (Semantic Versioning)

아래 버전들은 CalVer 전환 이전에 Semantic Versioning(`MAJOR.MINOR.PATCH`)으로 관리되었습니다.

### [2.4.0]

- 공지사항 기능 추가 (Rich Text, 팝업 안내)
- 점검서 양식 업로드 및 다운로드 기능 구현 (docxtemplater)
- 고객사별 누락 점검서 모니터링 기능 추가
- 진행 중인 이슈 알림 기능 추가

### [2.3.0]

- 고객사 관리 페이지 검색 및 필터 기능 추가
- 전체 고객사 요약 탭 추가 (하드웨어 포함 여부, 계약기간 표시)
- 담당자별 담당 고객사 수 기록 기능 추가
- 계약 정보에 하드웨어 포함 여부 항목 추가
- 미완료 고객사 대상 조회 기능 추가

### [2.2.0]

- 지원 로그 항목 세분화 및 예시 추가
- 지원 로그 엑셀 내보내기 중앙 정렬 개선
- 고객사 관리 페이지 뒤로가기 버튼 및 페이지 유지 개선
- 웹페이지 전환 시 시스템 로그에 로그아웃으로 기록되던 오류 수정
- 중복 고객사 생성 오류 및 세션 유지 오류 수정

### [2.1.0]

- 형상 관리(소스 버전 관리) 기능 추가
- 서버 정보 탭 추가
- 고객사 상세 탭 구조 개선 (기본정보/점검항목/소스관리/서버정보/지원로그)

### [2.0.0]

- NestJS + React + MariaDB 기반 전면 재구축
- JWT 인증 (Access/Refresh Token), Passport.js 적용
- Docker Compose 기반 배포 환경 구성
- 역할 기반 접근 제어 (super_admin / admin / user)
- 고객사 CRUD, 점검 항목, 지원 이력 관리 기능
