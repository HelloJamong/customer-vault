# ========================================
# 고객사 관리 시스템 환경 변수 설정
# ========================================
# 이 파일을 복사하여 .env 파일을 생성하고 각 환경에 맞게 수정하세요.
# 명령어: cp env.example .env

# ========== Flask 애플리케이션 설정 ==========

# Flask 환경 모드
# - development: 개발 환경 (자동 리로드, 상세한 에러 메시지)
# - production: 운영 환경 (최적화, 보안 강화)
FLASK_ENV=production

# Flask 디버그 모드
# - True: 개발 시에만 사용 (코드 변경 시 자동 재시작, 상세 에러 페이지)
# - False: 운영 환경에서 반드시 False (보안 위험)
FLASK_DEBUG=False

# Flask Secret Key (세션 암호화에 사용)
# ⚠️ 운영 환경에서는 반드시 무작위 문자열로 변경 필요!
# 생성 방법: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# ========== 데이터베이스 설정 (MariaDB) ==========

# MariaDB root 계정 비밀번호
# ⚠️ 운영 환경에서는 반드시 강력한 비밀번호로 변경!
DB_ROOT_PASSWORD=rootpassword

# 데이터베이스 이름
DB_NAME=customer_db

# 애플리케이션 전용 데이터베이스 사용자
DB_USER=customer_user

# 애플리케이션 데이터베이스 사용자 비밀번호
# ⚠️ 운영 환경에서는 반드시 강력한 비밀번호로 변경!
DB_PASSWORD=customerpass

# ========== 파일 업로드 설정 ==========

# 최대 업로드 파일 크기 (바이트 단위)
# 기본값: 16777216 (16MB)
# 예시:
#   - 10MB = 10485760
#   - 32MB = 33554432
#   - 50MB = 52428800
MAX_UPLOAD_SIZE=16777216

# 허용되는 파일 확장자 (쉼표로 구분)
# 현재는 PDF만 허용
ALLOWED_EXTENSIONS=pdf

# ========================================
# 운영 환경 배포 체크리스트
# ========================================
# [ ] SECRET_KEY를 무작위 문자열로 변경했는가?
# [ ] DB_ROOT_PASSWORD를 강력한 비밀번호로 변경했는가?
# [ ] DB_PASSWORD를 강력한 비밀번호로 변경했는가?
# [ ] FLASK_ENV=production으로 설정했는가?
# [ ] FLASK_DEBUG=False로 설정했는가?
# [ ] .env 파일이 .gitignore에 포함되어 있는가?
